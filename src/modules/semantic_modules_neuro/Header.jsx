import {Banner} from "../components/util/Banner.jsx";

export const Header = () => {
    return (
        <header className={"flex justify-center"}>
            <Banner></Banner>
        </header>
    )
}